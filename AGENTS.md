# Repository instructions

## Versioning

- Do not change the application version as part of ordinary edits, fixes, builds, or packaging.
- Update version fields such as `package.json` and `package-lock.json` only when the user explicitly requests a version update.
- A request to rebuild or package the application does not imply permission to update its version.
